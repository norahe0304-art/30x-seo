# Technical SEO Audit: https://www.launchpad.io

> Generated by `/seo technical https://www.launchpad.io`

---

## Technical Score: 74/100

### Category Breakdown

| Category | Status | Score |
|----------|--------|-------|
| Crawlability | OK | 90/100 |
| Indexability | WARN | 72/100 |
| Security | OK | 95/100 |
| URL Structure | OK | 88/100 |
| Mobile | OK | 82/100 |
| Core Web Vitals | WARN | 65/100 |
| Structured Data | WARN | 40/100 |
| JS Rendering | OK | 80/100 |

---

## 1. Crawlability (90/100)

### robots.txt
```
URL: https://www.launchpad.io/robots.txt
Status: 200 OK
```
```
User-agent: *
Allow: /
Disallow: /app/
Disallow: /api/
Disallow: /admin/
Sitemap: https://www.launchpad.io/sitemap.xml
```
- Valid syntax, sitemap referenced.
- `/app/` and `/api/` correctly blocked.

### XML Sitemap
```
URL: https://www.launchpad.io/sitemap.xml
Status: 200 OK
Type: Sitemap index
```
- Contains 3 child sitemaps: `pages.xml` (48 URLs), `blog.xml` (127 URLs), `docs.xml` (312 URLs).
- Total: 487 URLs indexed.
- **MEDIUM:** `blog.xml` includes 14 URLs returning 301 redirects. Remove redirected URLs from sitemap.

### Crawl Depth
- Homepage to deepest page: 4 clicks.
- 96% of pages reachable within 3 clicks.
- **LOW:** 19 documentation pages require 4+ clicks. Consider adding a docs hub page.

### AI Crawler Management
- robots.txt does not mention GPTBot, ClaudeBot, or other AI crawlers.
- For detailed AI crawler configuration, run `/seo geo-technical https://www.launchpad.io`.

---

## 2. Indexability (72/100)

### Canonical Tags
- All sampled pages have self-referencing canonical tags.

### Duplicate Content
- **HIGH:** `/features/` and `/product/` serve nearly identical content (92% similarity, 1,840 shared words). Consolidate or differentiate.
- **MEDIUM:** 8 blog posts have `?utm_source=...` parameter variants accessible without canonical pointing to clean URL. Confirmed canonicals are present and correct, but consider blocking parameters in robots.txt or using URL parameter handling.

### Thin Content
- **HIGH:** 6 documentation pages have fewer than 200 words:
  - `/docs/api/rate-limits` (87 words)
  - `/docs/api/errors` (112 words)
  - `/docs/changelog/2025-03` (45 words)
  - `/docs/changelog/2025-04` (62 words)
  - `/docs/changelog/2025-05` (58 words)
  - `/docs/changelog/2025-06` (71 words)
- Consider consolidating thin changelog pages into a single changelog or adding `noindex` to individual month pages.

### Pagination
- Blog uses `/blog?page=2` pattern. No `rel=next/prev` detected.
- **MEDIUM:** Add `rel=next`/`rel=prev` link elements or implement load-more with proper crawl paths.

### Index Bloat
- 487 URLs in sitemap, estimated 520+ indexable URLs.
- **MEDIUM:** `/blog/tag/*` pages (23 found) are thin aggregation pages. Consider `noindex` on low-value tag pages with fewer than 5 posts.

---

## 3. Security (95/100)

### HTTPS
- Enforced via 301 redirect from HTTP.
- SSL certificate valid (expires 2026-09-15, Let's Encrypt).
- No mixed content detected on sampled pages.

### Security Headers
| Header | Status | Value |
|--------|--------|-------|
| Strict-Transport-Security | OK | `max-age=31536000; includeSubDomains; preload` |
| Content-Security-Policy | OK | Present (policy reviewed, reasonable) |
| X-Frame-Options | OK | `DENY` |
| X-Content-Type-Options | OK | `nosniff` |
| Referrer-Policy | **MISSING** | Not set |
| Permissions-Policy | OK | Present |

- **LOW:** Add `Referrer-Policy: strict-origin-when-cross-origin` header.

---

## 4. URL Structure (88/100)

### URL Quality
- Clean, descriptive URLs throughout.
- Consistent lowercase, hyphen-separated.
- No query parameters on content pages.

### Redirects
- **MEDIUM:** 2 redirect chains detected:
  - `/product` -> `/product/` -> `/features/` (2 hops, should be 1)
  - `/blog/old-post-slug` -> `/blog/old-post-slug/` -> `/blog/new-post-slug` (2 hops)
- Update the first redirect in each chain to point directly to the final destination.

### Trailing Slashes
- Inconsistent. Feature pages use no trailing slash, blog posts use trailing slash.
- **MEDIUM:** Standardize trailing slash convention site-wide. Currently 301 redirects handle mismatches, but consistency is preferred.

---

## 5. Mobile Optimization (82/100)

### Viewport
```html
<meta name="viewport" content="width=device-width, initial-scale=1">
```
- Correctly configured.

### Touch Targets
- **HIGH:** Navigation menu items on mobile have 32x32px tap targets with 4px spacing. Minimum recommended: 48x48px with 8px spacing.

### Font Size
- Base font: 16px. Body text: 18px. No issues.

### Horizontal Scroll
- No horizontal overflow detected on 375px viewport.

### Mobile-First Indexing
- Google indexes all sites mobile-first as of July 5, 2024.
- Mobile version serves identical content to desktop. No content parity issues.

---

## 6. Core Web Vitals (65/100)

### CrUX Data (real user metrics, last 28 days)

| Metric | Value (p75) | Threshold | Status |
|--------|-------------|-----------|--------|
| LCP | 3.1s | < 2.5s | **FAIL** |
| INP | 180ms | < 200ms | PASS |
| CLS | 0.14 | < 0.1 | **FAIL** |

### LCP Analysis
- Largest element: Hero image on homepage (`hero-dashboard.webp`, 380KB).
- Server response time (TTFB): 620ms — acceptable but room for improvement.
- **HIGH:** LCP image lacks `fetchpriority="high"` and is not preloaded.
- **MEDIUM:** 4 render-blocking CSS files in `<head>` (combined 185KB). Consider inlining critical CSS.

### INP Analysis
- Passing at p75. No immediate action needed.
- One interaction outlier: pricing toggle (280ms) due to re-render of pricing table.

### CLS Analysis
- **HIGH:** Layout shift caused by web font loading (FOUT). `font-display: swap` is set but no `<link rel="preload">` for primary font.
- **MEDIUM:** Ad slot in blog sidebar injects content without reserved space.

---

## 7. Structured Data (40/100)

### Detected
| Type | Location | Valid |
|------|----------|-------|
| `WebSite` | Sitewide (JSON-LD) | Partial — missing `potentialAction` for sitelinks search |
| `Organization` | Homepage (JSON-LD) | Valid |

### Missing Opportunities

- **HIGH:** No `BreadcrumbList` on any page. Implement site-wide for enhanced SERP display.
- **HIGH:** Blog posts lack `Article` schema. Add `BlogPosting` with `author`, `datePublished`, `dateModified`, `image`.
- **MEDIUM:** Feature pages should have `SoftwareApplication` schema.
- **MEDIUM:** Pricing page should have `Offer` schema for product pricing.
- **LOW:** Add `potentialAction` (SearchAction) to `WebSite` schema for sitelinks search box eligibility.

---

## 8. JavaScript Rendering (80/100)

### Framework Detection
- Next.js (React) with server-side rendering (SSR).
- Initial HTML contains all critical content. No client-side-only rendering issues.

### SSR Verification
| Element | In Initial HTML | Status |
|---------|----------------|--------|
| Title tag | Yes | OK |
| Meta description | Yes | OK |
| H1 | Yes | OK |
| Body content | Yes | OK |
| Canonical | Yes | OK |
| Structured data | Yes | OK |
| Internal links | Yes | OK |

### JavaScript Issues
- **MEDIUM:** 2.1MB total JavaScript (compressed). Main bundle is 420KB. Consider code splitting for route-based chunks.
- **LOW:** 3 third-party scripts load synchronously: analytics, chat widget, A/B testing. Move to `async` or `defer`.

### IndexNow
- Not implemented.
- **LOW:** Consider adding IndexNow support for faster Bing/Yandex indexing of new blog posts and documentation updates.

---

## Critical Issues (fix immediately)

1. **Duplicate content:** `/features/` and `/product/` are 92% identical. Consolidate with a 301 redirect or substantially differentiate content.
2. **LCP failing (3.1s):** Preload hero image with `<link rel="preload">` and add `fetchpriority="high"`. Target < 2.5s.
3. **CLS failing (0.14):** Preload primary web font and reserve space for dynamic ad slots.

## High Priority (fix within 1 week)

4. Add `BreadcrumbList` schema site-wide.
5. Add `BlogPosting` schema to all blog posts.
6. Increase mobile nav touch targets to 48x48px minimum.
7. Remove 14 redirected URLs from `blog.xml` sitemap.

## Medium Priority (fix within 1 month)

8. Fix 2 redirect chains (reduce to single-hop 301s).
9. Consolidate thin changelog pages or add `noindex`.
10. Standardize trailing slash convention.
11. Add `rel=next`/`rel=prev` to blog pagination.
12. Inline critical CSS to reduce render-blocking resources.
13. Add `noindex` to low-value tag pages (< 5 posts).
14. Reserve space for blog sidebar ad slot (fixed height container).
15. Reduce JavaScript bundle size via code splitting.

## Low Priority (backlog)

16. Add `Referrer-Policy` security header.
17. Reduce crawl depth for 19 deep documentation pages.
18. Add `SearchAction` to `WebSite` schema.
19. Move third-party scripts to `async`/`defer`.
20. Implement IndexNow for Bing/Yandex.
